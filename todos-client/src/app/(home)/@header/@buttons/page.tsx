import { SyncButton } from "@/components";

const Buttons = () => {
    return (
        <div className="header__top__buttons">
            <SyncButton />
        </div>
    );
};

export default Buttons;