import { SyncTodosButton } from "@/components";

const Buttons = () => {
    return (
        <div className="header__top__buttons">
            <SyncTodosButton />
        </div>
    );
};

export default Buttons;