import { Todos } from "@/components";

const Buttons = () => {
    return (
        <div className="header__top__buttons">
            <Todos.SyncButton />
        </div>
    );
};

export default Buttons;