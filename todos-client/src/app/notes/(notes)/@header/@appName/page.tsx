import { Base } from "@/components";

const AppName = () => {
    return (
        <div className="header__top__app-name">
            <Base.Header.AppName baseClassName="header__top" />
        </div>
    );
};

export default AppName;