import { Base } from "@/components";

const Footer = () => {

    return (
        <footer className="footer">
            <Base.Footer.Copy baseClassName="footer" />
        </footer>
    );
};

export default Footer;